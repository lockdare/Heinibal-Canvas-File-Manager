# Heinibal's CardCanvas Plugin

这是一个创建card形式的canvas的插件，使用者可以通过card来管理所拥有的文件资产，构建其中的逻辑关系，并能轻易通过该canvas进行文件内容的更改。

This is a file card canvas view: display files as cards (title, tags, mod date). Click to edit in the right pane.
